deploy:
  commands:
    - name: echo something
      command: echo "deploying something in Okteto"
    - name: sleep
      command: echo "sleeping for 10 seconds" && sleep 10

destroy:
  commands:
    - name: echo something
      command: echo "destroying some external resource"
    - name: sleep
      command: echo "sleeping for 20 seconds" && sleep 20
    #- name: fail command
    #  command: asdgasgaerg
